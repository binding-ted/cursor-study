# Cursor 入门操作指南

> 面向新同事的快速上手文档 · 2026 版  
> 目标：30 分钟内会用 Cursor 完成日常「问问题、改代码、做小功能」

---

## 一、Cursor 是什么？

**Cursor** 是一款 **AI 代码编辑器**，基于 VS Code 改造而来。

- 界面、快捷键、插件和 VS Code **几乎一样**
- 额外内置了 AI：能读你的项目、改多个文件、跑命令、帮你查错
- 不用在浏览器和编辑器之间来回复制粘贴代码

**一句话理解：** 把 VS Code + ChatGPT 合在一起，且 AI 能直接动你的项目文件。

---

## 二、安装与首次打开

1. 打开官网：[https://cursor.com](https://cursor.com)
2. 下载 Windows 版并安装
3. 首次启动时：
   - 可导入 VS Code 的设置、插件（有就点「是」）
   - 登录 Cursor 账号（免费版即可上手）
4. 用 **文件 → 打开文件夹** 打开你的项目
5. 等待右下角索引完成（AI 需要先「读懂」项目）

---

## 三、界面长什么样？

打开后主要就三块：

| 区域 | 作用 |
|------|------|
| **左侧** | 文件树、搜索、Git、插件（和 VS Code 一样） |
| **中间** | 代码编辑区 |
| **右侧 / 底部** | **AI 对话面板**（Chat / Agent） |

> 新版 Cursor 还有 **Editor 视图** 和 **Agent 视图** 可切换：一边写代码，一边看 AI 改什么，建议并排使用。

---

## 四、四种最常用的 AI 操作

按「从小到大」排列，日常按任务大小选用即可。

### 1. Tab 自动补全（写代码时）

- **什么时候用：** 正常写代码，AI 会猜你下一行要写什么
- **怎么用：** 看到灰色提示 → 按 `Tab` 接受 → `Esc` 取消
- **特点：** 不打断思路，适合小片段补全

---

### 2. 内联编辑 `Ctrl + K`（改选中代码）

- **什么时候用：** 只改**当前文件里的一小段**代码
- **怎么用：**
  1. 用鼠标选中一段代码
  2. 按 `Ctrl + K`
  3. 输入指令，例如：「加注释」「改成 async/await」「解释这段代码」
  4. 预览 diff → 接受或拒绝
- **特点：** 精准、快，适合局部修改

---

### 3. 对话 Chat `Ctrl + L`（问问题、查代码）

- **什么时候用：** **只想问，不想改代码**
- **怎么用：**
  1. 按 `Ctrl + L` 打开对话
  2. 选择 **Ask（询问）模式**
  3. 提问，例如：
     - 「这个项目登录逻辑在哪？」
     - 「这个函数被谁调用了？」
     - 「帮我把这段报错原因讲清楚」
- **特点：** 安全，不会自动改文件，适合新人熟悉项目

---

### 4. Agent `Ctrl + I`（让 AI 帮你干活）

- **什么时候用：** 需要 **改多个文件、写新功能、跑命令、修 bug**
- **怎么用：**
  1. 按 `Ctrl + I` 打开 Agent 面板
  2. 用自然语言描述目标，例如：
     - 「给列表页加一个搜索框，样式跟现有按钮一致」
     - 「修一下 npm run build 的报错」
  3. AI 会读文件、改代码、必要时跑终端
  4. **你审核 diff（变更对比）后再点 Accept（接受）**
- **特点：** 最强大，也最需要人工把关

---

## 五、三种模式怎么选？

在 AI 面板里可以切换模式：

| 模式 | 会不会改代码 | 适合场景 |
|------|-------------|----------|
| **Ask（询问）** | 不会 | 问架构、查逻辑、学习代码 |
| **Agent（代理）** | 会 | 写功能、改 bug、批量重构 |
| **Plan（计划）** | 先规划再改 | 大功能、多文件改动（按 `Shift + Tab` 可切换） |

**推荐习惯：**

- 不熟悉项目 → 先用 **Ask** 问清楚
- 功能较大 → 先用 **Plan** 出方案，确认后再让 Agent 执行
- 小改动 → 直接用 **Agent** 或 `Ctrl + K`

---

## 六、@ 引用：让 AI 更准

在对话框里输入 `@`，可以手动指定上下文：

| 引用方式 | 示例 | 作用 |
|----------|------|------|
| `@文件名` | `@auth.ts` | 让 AI 重点看某个文件 |
| `@文件夹` | `@src/components` | 限定某个目录 |
| `@Codebase` | 全项目搜索 | 问「整个项目里 XXX 在哪」 |
| `@Web` | 联网搜索 | 查最新文档、API 说明 |
| `@Docs` | 官方文档 | 查框架官方说明 |

**技巧：** 问题越具体 + `@` 引用越准，AI 越少瞎编。

---

## 七、写清楚需求（Prompt 小技巧）

好的描述 = **做什么 + 在哪做 + 约束条件**

**❌ 不好：** 「优化一下代码」

**✅ 好：** 「在 `UserList.tsx` 里给表格加一列「注册时间」，格式 YYYY-MM-DD，不要改现有接口字段名」

**再举几个模板：**

```
帮我在 src/api/ 下新建 user.ts，封装 GET /users 接口，用项目现有的 request 工具。
```

```
这个报错是什么原因？[粘贴报错]
请只改最小范围，不要重构无关文件。
```

```
先不要写代码，告诉我登录流程涉及哪些文件、数据怎么流转。
```

---

## 八、改完代码必做：Review（审核）

Agent 改完会显示 **diff（差异对比）**：

- **绿色** = 新增
- **红色** = 删除
- 可以 **逐文件 Accept / Reject**
- 不确定的地方 → 切到 Ask 模式问：「你为什么这样改？」

**新人原则：看不懂的改动，不要点 Accept。**

---

## 九、和 Git 配合（强烈建议）

1. 动手前先 `git checkout -b feature/xxx` 开分支
2. 让 AI 改完 → 本地跑一下 / 看报错
3. 确认无误再 commit

这样 AI 改错了可以一键回滚，不会污染主分支。

---

## 十、项目规则（可选，团队统一风格时用）

项目里可以放 `.cursor/rules/` 或在根目录写 `AGENTS.md`，告诉 AI：

- 用什么框架、命名规范
- 哪些文件不能动
- 回复用什么语言（如：始终用中文）

**你不需要第一天就配这个**，知道有这回事即可；团队有规范时再一起加。

---

## 十一、Windows 常用快捷键速查

| 快捷键 | 功能 |
|--------|------|
| `Ctrl + L` | 打开 AI 对话 |
| `Ctrl + I` | 打开 Agent（多文件任务） |
| `Ctrl + K` | 内联编辑（选中代码后） |
| `Ctrl + Shift + L` | 把选中代码加入对话 |
| `Tab` | 接受 AI 补全建议 |
| `Esc` | 取消 AI 建议 |
| `Shift + Tab` | 在 Agent 面板切换 Plan 等模式 |

> Mac 用户把 `Ctrl` 换成 `Cmd` 即可。

---

## 十二、常见问题

**Q：AI 改错了怎么办？**  
A：`Ctrl + Z` 撤销，或 Git 丢弃改动；下次描述更具体，或用 `@` 指定文件。

**Q：免费版够用吗？**  
A：学习、日常小任务够用；高频 Agent 可能需要 Pro 订阅，看团队账号安排。

**Q：代码会被拿去训练吗？**  
A：可在设置里开启 **Privacy Mode（隐私模式）**；公司项目建议跟团队安全规范走。

**Q：和 VS Code 插件兼容吗？**  
A：大部分兼容，常用插件可直接装。

**Q：什么时候不要用 Agent？**  
A：生产环境配置、删库、改权限、不熟悉的大重构——先 Ask 问清楚，或找同事确认。

---

## 十三、新人第一天练习清单

按顺序做一遍，基本就上手了：

- [ ] 安装 Cursor，打开一个项目文件夹
- [ ] 用 **Ask 模式** 问：「这个项目目录结构是怎样的？」
- [ ] 选中 10 行代码，用 **Ctrl + K** 让它「加中文注释」
- [ ] 用 **Agent** 做一个小改动（如改按钮文字、加一行日志）
- [ ] **Review diff**，只 Accept 看得懂的改动
- [ ] 在对话里试一次 `@某个文件名` 提问

---

## 十四、推荐工作流（一张图）

```
小补全        → Tab 自动补全
改一小段      → Ctrl + K
问问题/学代码 → Ctrl + L（Ask 模式）
做功能/修 bug → Ctrl + I（Agent 模式）
大功能        → Plan 模式先出方案 → 再 Agent 执行
改完必做      → Review diff → 本地验证 → Git 提交
```

---

**有问题随时找带你的同事；Cursor 本身也在快速更新，界面可能略有变化，但上面这套「Ask → Plan → Agent → Review」的主线不会变。**

祝上手顺利。
